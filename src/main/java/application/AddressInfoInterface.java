package application;

/**This is the interface with the method 
 * to getAddress**/
public interface AddressInfoInterface {
	
	/*This is the abstract methos that needs to be overriden 
	 * for different API to send the address*/
	public String getAddress();
	
}
